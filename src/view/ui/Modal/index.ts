import {
  ModalBase,
  ModalDescription,
  ModalFooter,
  ModalHeader,
  ModalTitle,
} from './Modal';

export const Modal = {
  Root: ModalBase,
  Header: ModalHeader,
  Footer: ModalFooter,
  Title: ModalTitle,
  Description: ModalDescription,
};

export type { ModalProps } from './Modal';
