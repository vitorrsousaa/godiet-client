import {
  ModalBase,
  ModalDescription,
  ModalFooter,
  ModalHeader,
  ModalTitle,
} from './Modal';

export const Modal = {
  Root: ModalBase,
  Header: ModalHeader,
  Footer: ModalFooter,
  Title: ModalTitle,
  Description: ModalDescription,
};

export { ModalContent, ModalRoot } from './Modal';

export type { ModalProps } from './Modal';
