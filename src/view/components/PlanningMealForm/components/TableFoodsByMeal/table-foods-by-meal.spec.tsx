import { clearAllMocks } from '@testing-suit';

describe('TableFoodsByMeal', () => {
  beforeEach(() => {
    clearAllMocks();
  });

  it('should render TableFoodsByMeal', () => {
    expect(true).toBeTruthy();
  });
});
